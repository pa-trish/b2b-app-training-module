services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: training
      POSTGRES_PASSWORD: training
      POSTGRES_DB: training_module
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
